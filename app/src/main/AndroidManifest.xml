<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gh.edu.ktu.myktu">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ktu_logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/ktu_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FinancialSupportActivity" />
        <activity android:name=".AdmissionActivity" />
        <activity android:name=".MailActivationActivity" />
        <activity android:name=".MailActivity" />
        <activity android:name=".AttachmentActivity" />
        <activity android:name=".ExamTimeTableActivity" />
        <activity android:name=".VirtualLearningEnvironmentActivity" />
        <activity android:name=".UniversityLibraryActivity" />
        <activity android:name=".TeachingTimeTableActivity" />
        <activity android:name=".FeeStructureActivity" />
        <activity android:name=".OsisActivity" />
        <activity android:name=".AlumniActivity" />
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>